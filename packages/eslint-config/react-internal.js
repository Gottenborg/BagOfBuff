import reactHooks from "eslint-plugin-react-hooks";
import reactRefresh from "eslint-plugin-react-refresh";
import turboPlugin from "eslint-plugin-turbo";
import globals from "globals";

import { config as baseConfig } from "./base.js";

/**
 * ESLint configuration for the React apps (storefront + back office).
 *
 * Extends the shared base config with browser globals and React-specific
 * rules (hooks correctness + fast-refresh boundaries).
 *
 * @type {import("eslint").Linter.Config[]}
 */
export const config = [
  ...baseConfig,
  {
    languageOptions: {
      globals: {
        ...globals.browser,
        ...globals.serviceworker,
      },
    },
  },
  {
    plugins: {
      "react-hooks": reactHooks,
    },
    rules: {
      ...reactHooks.configs.recommended.rules,
    },
  },
  {
    plugins: {
      "react-refresh": reactRefresh,
    },
    rules: {
      "react-refresh/only-export-components": [
        "warn",
        { allowConstantExport: true },
      ],
    },
  },
  {
    plugins: {
      turbo: turboPlugin,
    },
    rules: {
      // Allow Vite's built-in `import.meta.env` flags — they are compile-time
      // constants, not turbo-tracked environment variables.
      "turbo/no-undeclared-env-vars": [
        "warn",
        { allowList: ["^(DEV|PROD|MODE|SSR|BASE_URL)$"] },
      ],
    },
  },
  {
    // Generated by TanStack Router — not hand-edited, so don't lint it.
    ignores: ["**/routeTree.gen.ts", ".output/**", ".nitro/**"],
  },
];
