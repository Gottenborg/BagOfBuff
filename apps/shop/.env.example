# API base URL the storefront talks to.
VITE_API_URL="http://localhost:3001"

# Optional: Plausible analytics domain (cookieless). When set, and once the
# visitor accepts analytics in the consent banner, the Plausible script loads.
# Leave unset to disable analytics entirely.
VITE_PLAUSIBLE_DOMAIN="bagofbuff.com"
